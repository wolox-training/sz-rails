language: ruby
rbenv:
  - 2.5.1

services:
  - postgresql

before_install:
  - gem update --system
  - bundle install

before_script:
  - bundle exec rake db:migrate

script:
  - bundle exec rubocop app spec -R #--format simple

cache:
  bundler: true
  